# packages/deploy

**v1 scope:** Flash student artifact to mici. **Always gated** — API/UI require explicit confirm; never auto-flash.

**M0:** stub only — demo pauses at gated flash; confirm advances to done.
