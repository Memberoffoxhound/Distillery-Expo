# packages/student

**v1 scope:** Train lighter student with **stock modelV2 I/O** (tinygrad) targeting mici/QCOM.

**Design lock:** I/O compatible with stock modelV2 — no custom cam/control contracts in v1.

**M0:** stub only — demo emits fake train loss curve.
